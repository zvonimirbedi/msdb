package com.example.msdb.Repository;

import com.example.msdb.Entity.Media;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryMedia extends JpaRepository<Media, Integer> {
}
